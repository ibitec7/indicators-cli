[project]
name = "indicators-cli"
version = "0.1.0"
description = "A CLI tool to automate the process of generating technical indicators for stock market data. "
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.3.1",
    "polars>=1.39.3",
    "setuptools>=82.0.1",
    "yfinance>=1.2.0",
]
